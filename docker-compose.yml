version: "3.0"

services:
  greeter:
    build:
      context: .
    image: gds-service-greeter
    env_file: docker-compose.env
    environment:
      SERVICES: greeter
    external_links:
      - redis:redis
      - mongo:mongo
      - nats:nats
      - logstash:logstash
    network_mode: bridge
